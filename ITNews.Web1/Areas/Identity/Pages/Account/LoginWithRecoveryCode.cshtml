@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Код восстановления";
}

<h2>@ViewData["Title"]</h2>
<hr />
<p>
    Вы запросили войти в систему с кодом восстановления. Этот логин не будет сохранен до тех пор, пока вы не введете
    код приложения-аутентификатора при входе в систему или не отключите 2FA и не войдете в систему снова.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.RecoveryCode"></label>
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Авторизоваться</button>
        </form>
    </div>
</div>
 
 @section Scripts {
    <partial name="_ValidationScriptsPartial" />
}